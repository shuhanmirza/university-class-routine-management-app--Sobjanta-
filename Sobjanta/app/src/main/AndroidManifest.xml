<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codeian.sobjanta">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivityMain"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityLogin"
            android:theme="@style/AuthTheme" />
        <activity
            android:name=".ActivitySignUp"
            android:theme="@style/AuthTheme" />
        <activity android:name=".ActivityAdmin" />
        <activity android:name=".ActivityAddCourse" />
        <activity android:name=".ActivityAddTeacher" />
        <activity android:name=".ActivityAddCr" />
        <activity android:name=".ActivityRemoveTeacher" />
        <activity android:name=".ActivityRemoveCr" />
        <activity android:name=".ActivityRemoveCourse"></activity>
    </application>

</manifest>